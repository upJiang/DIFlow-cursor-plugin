---
description: 
globs: 
alwaysApply: true
---
请不要添加测试代码
node工具使用yarn，不得使用npm
模块清晰，相关内容放到相关目录，不要乱起目录
每一个函数方法都要有对函数的解释，包括入参，出参等
```
/**
 * 函数解释
 */
```
每次新增功能时不能影响已有功能，除非有特定说明
每次更新env文件的时候，不要重置已有的变量值
开发相同功能时，替换方案要把原来方案的代码、依赖删除
删除某个功能点时，需要把对应的文件夹、文件、依赖都删掉
新增或修改功能时不要影响已有功能
如果贴了api接口给你，请不要自己乱定义接口
在每次回答之前都先通读一下相关代码
修改指令后不需要重新构建
重新执行项目的时候，直接执行 yarn dev:all 就行了